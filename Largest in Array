
class Solution
{
public:
    int largest(vector<int> &arr)
    {
        // code here
        int n = arr.size();
        int x = -1;
        for (int i = 0; i < n; i++)
        {
            if (arr[i] > x)
                x = arr[i];
        }
        return x;
    }
};
